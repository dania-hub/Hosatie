export default {
    plugins: {
        // Tailwind v4 moved its PostCSS plugin into the separate package
        // '@tailwindcss/postcss'. Use the package below (installed as devDependency).
        '@tailwindcss/postcss': {},
        autoprefixer: {},
    },
}